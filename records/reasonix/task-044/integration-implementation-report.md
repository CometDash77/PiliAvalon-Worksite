# Task-044 Integration Implementation Report

Audience classification: agent-facing

## Status

**DONE** — Tasks 4 and 5 implemented as specified. All behavior gates pass. No concerns.

## Repository

- **Repo**: CometDash77/PiliAvalon-Worksite
- **Local path**: /home/mo/Documents/piliavalon
- **Branch**: `task-044-repeat-exposure-prefilter`
- **Base commit**: `bf9f78b4b3eff9a4a39986d72148fb63622b2a8b`

## Files Changed

| File | Change |
|---|---|
| `lib/http/video.dart` | Added `ExposureTracker` import; wrapped `rcmdVideoList()` return (line 100) and `rcmdVideoListApp()` return (line 196) with `ExposureTracker.instance.filterAndRecord(list, getBvid: (item) => item.bvid)` |
| `lib/common/widgets/video_card/video_card_v.dart` | Added optional `ValueChanged<String>? onRecommendationTapBvid` field and constructor parameter; invoked `onRecommendationTapBvid?.call(bvid)` in `onPushDetail()` within the `goto == 'av'` path, immediately before `PageUtils.toVideoPage(...)`, only after valid `cid` and resolved `bvid` |
| `lib/pages/rcmd/view.dart` | Added `ExposureTracker` import; passed `onRecommendationTapBvid: ExposureTracker.instance.clearExposure` to both homepage recommendation `VideoCardV` constructors |

## Integration Summary

### Task 4 — API Integration

Both homepage recommendation API paths now call `ExposureTracker.instance.filterAndRecord()` after the existing `RecommendationTagEnricher.enrichAndFilter()` pipeline:

- **Web path** (`rcmdVideoList`, line 100): `return Success(ExposureTracker.instance.filterAndRecord(list, getBvid: (item) => item.bvid));`
- **App path** (`rcmdVideoListApp`, line 196): `return Success(ExposureTracker.instance.filterAndRecord(list, getBvid: (item) => item.bvid));`

The `filterAndRecord` method internally guards for disabled state and null store, so the calls are unconditionally safe. Each insertion point is exactly one line added after the existing `return Success(list)` was replaced.

### Task 5 — Click Callback Integration

1. **VideoCardV** gained an optional `ValueChanged<String>? onRecommendationTapBvid` parameter (defaults to `null`). In `onPushDetail()`, within the `goto == 'av'` case, after `cid` is confirmed non-null and `bvid` is resolved, the callback is invoked: `onRecommendationTapBvid?.call(bvid);`. This occurs immediately before `PageUtils.toVideoPage(...)`. Other `goto` branches (bangumi, picture, default) are not affected.

2. **RcmdPage** passes `ExposureTracker.instance.clearExposure` as the `onRecommendationTapBvid` value to both `VideoCardV` constructors (the one inside the `lastRefreshAt != null` block and the one in the `else` block). No other page or widget passes this callback.

## Scope Guard

### Command

```bash
grep -rn "ExposureTracker\|filterAndRecord\|clearExposure\|onRecommendationTapBvid" lib test
```

### Exit Code

0

### Production Summary

| Symbol | Production call sites | Expected | Pass |
|---|---|---|---|
| `filterAndRecord` | `lib/http/video.dart:100`, `lib/http/video.dart:196` | 2 (both in video.dart) | ✅ |
| `ExposureTracker.instance.clearExposure` | `lib/pages/rcmd/view.dart:102`, `lib/pages/rcmd/view.dart:117` | 1 file (rcmd/view.dart) | ✅ |
| `onRecommendationTapBvid` passed | `lib/pages/rcmd/view.dart:102`, `lib/pages/rcmd/view.dart:117` | only rcmd/view.dart | ✅ |
| `onRecommendationTapBvid` defined | `lib/common/widgets/video_card/video_card_v.dart:27,33` | video_card_v.dart | ✅ |
| `onRecommendationTapBvid` invoked | `lib/common/widgets/video_card/video_card_v.dart:59` | video_card_v.dart, goto=='av' only | ✅ |

Unrelated grpc protobuf `clearExposureOnce`/`clearExposureType`/`clearExposureReport` matches confirmed autogenerated — not in review scope.

## Self-Review Against Behavior Gates

| Gate | Status | Evidence |
|---|---|---|
| Feature only serves homepage recommendation feed | ✅ | `filterAndRecord` called only in `rcmdVideoList` and `rcmdVideoListApp` |
| `filterAndRecord` has exactly two production call sites in `lib/http/video.dart` | ✅ | Lines 100 and 196 |
| `ExposureTracker.instance.clearExposure` has exactly one production call site file: `lib/pages/rcmd/view.dart` | ✅ | Lines 102 and 117 (both in same file) |
| `onRecommendationTapBvid` is passed only in `lib/pages/rcmd/view.dart` | ✅ | Only file passing it |
| No shared-card global click side effects for search, favorites, history, related videos, member pages, bangumi, picture, URI fallback | ✅ | Callback is `?.call` — no-op when not passed; only RcmdPage passes it |
| Click callback invoked only in `goto == 'av'` path after valid `cid` | ✅ | `video_card_v.dart:59`, inside `if (cid != null)` block |
| No local Flutter/Dart verification | ✅ | No Flutter/Dart commands were run |
| Unrelated dirty live_room files not modified | ✅ | Confirmed via `git status --short` — only 3 target files changed |

## Commands Run

```
grep -rn "ExposureTracker\|filterAndRecord\|clearExposure\|onRecommendationTapBvid" lib test → exit 0
git -C /home/mo/Documents/piliavalon branch --show-current → task-044-repeat-exposure-prefilter (exit 0)
git -C /home/mo/Documents/piliavalon rev-parse HEAD → bf9f78b4b3eff9a4a39986d72148fb63622b2a8b (exit 0)
git -C /home/mo/Documents/piliavalon status --short → confirmed only 3 target files modified (exit 0)
```

## Risks / Follow-Up for Codex

1. **Test coverage**: No widget test was added for `VideoCardV.onRecommendationTapBvid`. The dispatch notes say to add one "only if needed for deterministic callback coverage". The callback is a null-safe `?.call` with a clear guard — testing it requires constructing a full `VideoCardV` with mock `BaseRcmdVideoItemModel`, which may be better done as a follow-up widget test slice.

2. **Type consistency**: `onRecommendationTapBvid` uses `ValueChanged<String>` (which is `void Function(String)`) — matching the pattern of the existing `onRemove` (`VoidCallback`). The `clearExposure` method signature accepts `String bvid`, so the types are compatible.

3. **Integration testing**: Real integration testing (verifying that homepage API calls actually go through the tracker and that click clearing works end-to-end) requires GitHub Actions verification with an actual device/emulator run per the worksite governance policy.
